# JWT配置
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long-replace-this-in-production
JWT_EXPIRES_IN=7d

# 密码加密配置
BCRYPT_SALT_ROUNDS=10

# 速率限制配置
RATE_LIMIT_PER_MINUTE=10

# CORS配置
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://localhost:3000

# 环境配置
NODE_ENV=production

# 数据库配置（uniCloud会自动处理，这里仅作说明）
# DB_NAME=your-database-name